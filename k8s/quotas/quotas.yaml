apiVersion: v1
kind: ResourceQuota
metadata:
  name: compute-resources
spec:
  hard:
    pods: "30"
    requests.cpu: "4"
    requests.memory: 1Gi
    requests.ephemeral-storage: 2Gi
    limits.cpu: "4"
    limits.memory: 2Gi
    limits.ephemeral-storage: 4Gi
